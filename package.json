{
  "name": "2nth-demo",
  "version": "1.0.0",
  "private": true,
  "description": "demo.2nth.ai — AI-powered ERP demonstrations",
  "scripts": {
    "dev": "npx wrangler pages dev ./public",
    "deploy": "npx wrangler pages deploy ./public --project-name=2nth-demo --branch=main"
  }
}
